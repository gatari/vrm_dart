name: vrm_dart_example
version: 1.0.0+0
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
      sdk: flutter

  path: ^1.8.0
  json_annotation:
  vrm_dart:
    path:
      ..
  cupertino_icons: ^1.0.3
  flutter_hooks: ^0.18.0
  hooks_riverpod: ^1.0.0-dev.7
  file_picker: ^4.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  json_serializable:
  lints: ^1.0.1
